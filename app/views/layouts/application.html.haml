!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'GSOH: Great Sense of Home'
    = Gon::Base.render_data
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Helping you find that perfect place to live in Leeds'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = favicon_link_tag
    = csrf_meta_tags
    = metamagic og: og_tags, twitter: twitter_tags
    = analytics_init(skip_pageview: true) if GoogleAnalytics.valid_tracker?
  %body
    %header{class: controller_name}
      = render 'layouts/nav'
    %main{:role => "main", class: controller_name}
      = yield :outside_container
      #content
        = render 'layouts/messages'
        = yield
        .push
    %footer
      .pull-right
        Developed by #{link_to 'Fish Percolator', 'https://fishpercolator.co.uk/'} as part of #{link_to 'Urban Sustainable Development Lab', 'http://dhyaandesign.com/sustainabledevelopmentlab/'} (#{link_to 'about this site', page_path('about')})